# ADR-0005: Optional transaction title

Category is the required classification field; title is optional descriptive context. An empty title is stored as an empty string, and transaction lists use the category name as the primary display text when no title is present.
